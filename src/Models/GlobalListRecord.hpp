#pragma once

#include <string>

struct GlobalListRecord {
    int id;
    int percent;
    std::string status;
    std::string videoURL;
    int internalID;
    std::string levelName;
    int placement;
};